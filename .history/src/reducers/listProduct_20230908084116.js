import products from "../data/products.json";
export const listProduct = (state = products, action) => {
  console.log(action);
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
